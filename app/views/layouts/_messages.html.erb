<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %>">
    <a href="#" data-dismiss="alert" class="close">×</a>
      <ul>
      	<% if !key = notice %>
	        <li>
	          <%= value %>
	        </li>
	    <% else %>
	    	<li>
	          <%= value %>
	        </li>
	    <% end %>
      </ul>
  </div>
<% end %>